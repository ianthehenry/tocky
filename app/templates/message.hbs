{{#unless isRepeatSender}}
  <div class="row">
    <div class="gutter">
      {{-avatar user=sender}}
    </div>
    <div class="alley">
      <div class="message-timestamp">{{date}}</div>
      <span class="sender">{{sender.name}}</span>
    </div>
  </div>
{{/unless}}
<div class="row">
  <div class="gutter status unread">
  </div>
  <div class="alley message-content">
    {{#if showTimeStamp}}
      <div class="message-timestamp">{{time}}</div>
    {{/if}}
    {{{htmlContent}}}
  </div>
</div>